mixin imgLazy(src, alt)
	img(class="lazy", data-src=src, src="/local/assets/img/placeholder.png" alt=alt)&attributes(attributes)

mixin pictureLazy(src, alt, srcPlacegolder="/local/assets/img/placeholder.png")
	-const srcParts = src.split(".")
	-const [srcFirstPart, extname] = srcParts

	picture
		source(type="image/webp", data-srcset="/local/assets/img/" + `${srcFirstPart}.webp`, srcset=srcPlacegolder)
		img(class="lazy", data-src="/local/assets/img/" +`${srcFirstPart}.${extname}`, alt=alt)&attributes(attributes)


mixin picture(src, alt)
	-const srcParts2 = src.split(".")
	-const [srcFirstPart2, extname2] = srcParts2

	picture
		source(type="image/webp", srcset="/local/assets/img/" + `${srcFirstPart2}.webp`)
		img(src="/local/assets/img/" + `${srcFirstPart2}.${extname2}`, alt=alt)&attributes(attributes)
